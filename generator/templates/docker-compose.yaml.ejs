version: '2'
services:
    <%- name %>:
        build:
            context: .
            dockerfile: ./Dockerfile
        container_name: <%- name %>
        environment:
        - "NODE_ENV=production"
        ports:
        - <%- port %>:<%- port %>
        user: node
        working_dir: /app
        command: node dist/src/server
